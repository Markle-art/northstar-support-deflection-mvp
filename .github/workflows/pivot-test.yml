name: Meridian Pivot Test

on:
  workflow_dispatch:

jobs:
  test-pivot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Find app.py
        run: |
          echo "Searching for app.py..."
          find . -name "app.py" -type f -print
